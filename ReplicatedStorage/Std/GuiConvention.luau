--[[
================================================================================
KOSMICMAZER — GUI Convention
================================================================================

Purpose: Стандарти оформлення графічного інтерфейсу користувача (GUI)
Version: 1.0
Status: ACTIVE
Last Updated: 2026-01-07

================================================================================
1. ЗАСТАВКА ПОЧАТКУ ГРИ (SPLASH SCREEN)
================================================================================

Реалізація: ServerScriptService/GameInit.server.luau

1.1 ОБОВ'ЯЗКОВІ ЕЛЕМЕНТИ:
--------------------------
- Логотип гри "KOSMICMAZER" по центру екрану
- Аватар гравця з анімацією появи
- Ім'я гравця
- Progress bar завантаження
- Текст статусу завантаження (укр.)

1.2 ПОСЛІДОВНІСТЬ ЗАВАНТАЖЕННЯ:
--------------------------------
1. "🔍 Перевірка профілю..."
2. "📊 Завантаження даних..."
3. "🌍 Підготовка середовища..."
4. "🚀 Запуск гри..."

1.3 СТИЛІЗАЦІЯ:
----------------
- Фон: Темний напівпрозорий (BackgroundColor3 = Color3.new(0, 0, 0))
- Прозорість фону: 0.3
- Логотип: Білий текст, жирний шрифт
- Progress bar: Синій (#3498db) на темному фоні
- Аватар: Круглий з рамкою, анімація масштабування
- Анімації: Плавні переходи (TweenService)

1.4 ПРИКЛАД СТРУКТУРИ:
-----------------------
ScreenGui "LoadingScreen"
├── Frame "Background"
│   ├── TextLabel "Logo" -- "KOSMICMAZER"
│   ├── ImageLabel "Avatar" -- Круглий аватар гравця
│   ├── TextLabel "PlayerName"
│   ├── Frame "ProgressBarContainer"
│   │   └── Frame "ProgressBarFill"
│   └── TextLabel "StatusText"

================================================================================
2. ДІАЛОГОВІ ВІКНА
================================================================================

Підтримуються два ретро-стилі діалогових вікон:
- Windows 95 Style (основний)
- OS/2 Warp Style (альтернативний)

================================================================================
2.1 ДІАЛОГОВІ ВІКНА WINDOWS 95 STYLE
================================================================================

Класичний стиль Windows 95 з характерними 3D рамками.

2.1.1 КОЛЬОРОВА ПАЛІТРА WINDOWS 95:
------------------------------------
local WIN95_COLORS = {
    -- Фони
    WindowBackground = Color3.fromRGB(192, 192, 192),  -- Сірий фон вікна
    TitleBarActive = Color3.fromRGB(0, 0, 128),        -- Синій заголовок (активний)
    TitleBarInactive = Color3.fromRGB(128, 128, 128),  -- Сірий заголовок (неактивний)
    
    -- Текст
    TitleText = Color3.fromRGB(255, 255, 255),         -- Білий текст заголовка
    WindowText = Color3.fromRGB(0, 0, 0),              -- Чорний текст вікна
    DisabledText = Color3.fromRGB(128, 128, 128),      -- Сірий (вимкнений)
    
    -- Кнопки
    ButtonFace = Color3.fromRGB(192, 192, 192),        -- Сірий фон кнопки
    ButtonHighlight = Color3.fromRGB(255, 255, 255),   -- Білий (світла рамка)
    ButtonShadow = Color3.fromRGB(128, 128, 128),      -- Темно-сірий (тінь)
    ButtonDarkShadow = Color3.fromRGB(0, 0, 0),        -- Чорний (глибока тінь)
    
    -- Акценти
    Highlight = Color3.fromRGB(0, 0, 128),             -- Синій виділення
    HighlightText = Color3.fromRGB(255, 255, 255),     -- Білий на виділенні
}

2.1.2 СТРУКТУРА ВІКНА WIN95:
-----------------------------
Frame "DialogWindow"
├── Frame "TitleBar" (Height: 20px, синій градієнт)
│   ├── ImageLabel "Icon" (16x16, зліва)
│   ├── TextLabel "Title" (білий текст)
│   ├── TextButton "MinimizeBtn" -- "_" (квадратна)
│   ├── TextButton "MaximizeBtn" -- "□" (квадратна)
│   └── TextButton "CloseBtn" -- "X" (квадратна)
├── Frame "Content" (сірий фон)
│   └── [Вміст вікна]
└── Frame "StatusBar" (опціонально)
    └── TextLabel "Status"

2.1.3 ХАРАКТЕРИСТИКИ WIN95:
----------------------------
- Рамка: 3D ефект (світла зверху/зліва, темна знизу/справа)
- Заголовок: Синій градієнт зліва направо
- Шрифт: SourceSans (аналог MS Sans Serif)
- Кнопки: Квадратні 16x14px з 3D ефектом
- Тінь тексту: Немає

================================================================================
2.2 ДІАЛОГОВІ ВІКНА OS/2 WARP STYLE
================================================================================

Альтернативний стиль OS/2 Warp 4 з характерними елементами.

2.2.1 КОЛЬОРОВА ПАЛІТРА OS/2 WARP:
-----------------------------------
local OS2_COLORS = {
    -- Фони
    WindowBackground = Color3.fromRGB(204, 204, 204),  -- Світло-сірий фон
    TitleBarActive = Color3.fromRGB(0, 64, 128),       -- Темно-синій заголовок
    TitleBarInactive = Color3.fromRGB(96, 96, 96),     -- Темно-сірий (неактивний)
    
    -- Текст
    TitleText = Color3.fromRGB(255, 255, 255),         -- Білий текст заголовка
    WindowText = Color3.fromRGB(0, 0, 0),              -- Чорний текст вікна
    DisabledText = Color3.fromRGB(128, 128, 128),      -- Сірий (вимкнений)
    
    -- Кнопки
    ButtonFace = Color3.fromRGB(204, 204, 204),        -- Світло-сірий фон
    ButtonHighlight = Color3.fromRGB(255, 255, 255),   -- Білий (світла рамка)
    ButtonShadow = Color3.fromRGB(128, 128, 128),      -- Сірий (тінь)
    ButtonDarkShadow = Color3.fromRGB(64, 64, 64),     -- Темно-сірий
    
    -- Акценти OS/2
    IconBarBackground = Color3.fromRGB(0, 64, 128),    -- Синій для icon bar
    SystemMenu = Color3.fromRGB(255, 255, 0),          -- Жовтий акцент
    ScrollbarThumb = Color3.fromRGB(0, 128, 128),      -- Бірюзовий scrollbar
}

2.2.2 СТРУКТУРА ВІКНА OS/2:
----------------------------
Frame "DialogWindow"
├── Frame "TitleBar" (Height: 22px, темно-синій)
│   ├── Frame "SystemMenuIcon" (іконка зліва, клікабельна)
│   ├── TextLabel "Title" (білий текст, по центру)
│   ├── TextButton "MinimizeBtn" -- "▼" (трикутник вниз)
│   ├── TextButton "MaximizeBtn" -- "▲" (трикутник вгору)
│   └── TextButton "CloseBtn" -- горизонтальна лінія
├── Frame "IconBar" (опціонально, під заголовком)
│   └── [Іконки швидкого доступу]
├── Frame "Content"
│   └── [Вміст вікна]
└── Frame "StatusBar"
    └── TextLabel "Status"

2.2.3 ХАРАКТЕРИСТИКИ OS/2 WARP:
--------------------------------
- Рамка: Тонша 3D рамка, менш виражена
- Заголовок: Суцільний темно-синій, БЕЗ градієнта
- Шрифт: Трохи більший, напівжирний для заголовка
- Кнопки керування: Трикутні символи замість квадратних
- System Menu: Жовта іконка зліва
- Icon Bar: Додаткова панель під заголовком (унікально для OS/2)

2.2.4 КНОПКИ КЕРУВАННЯ OS/2:
-----------------------------
- Minimize: ▼ (трикутник вниз) - згортає вікно
- Maximize: ▲ (трикутник вгору) - розгортає вікно
- Close: — (горизонтальна лінія) - закриває вікно

2.2.5 ПРИКЛАД КОДУ OS/2 ДІАЛОГУ:
---------------------------------
local function createOS2Dialog(title, message, dialogType)
    local dialog = Instance.new("Frame")
    dialog.BackgroundColor3 = OS2_COLORS.WindowBackground
    dialog.BorderSizePixel = 2
    dialog.BorderColor3 = OS2_COLORS.ButtonShadow
    dialog.Size = UDim2.new(0, 320, 0, 160)
    dialog.Position = UDim2.new(0.5, -160, 0.5, -80)
    
    -- Title bar (темно-синій, без градієнта)
    local titleBar = Instance.new("Frame")
    titleBar.BackgroundColor3 = OS2_COLORS.TitleBarActive
    titleBar.Size = UDim2.new(1, 0, 0, 22)
    titleBar.BorderSizePixel = 0
    titleBar.Parent = dialog
    
    -- System menu icon (жовтий квадрат зліва)
    local sysMenu = Instance.new("Frame")
    sysMenu.BackgroundColor3 = OS2_COLORS.SystemMenu
    sysMenu.Size = UDim2.new(0, 16, 0, 16)
    sysMenu.Position = UDim2.new(0, 3, 0, 3)
    sysMenu.Parent = titleBar
    
    -- Title text (по центру)
    local titleLabel = Instance.new("TextLabel")
    titleLabel.Text = title
    titleLabel.TextColor3 = OS2_COLORS.TitleText
    titleLabel.Font = Enum.Font.SourceSansSemibold
    titleLabel.TextSize = 14
    titleLabel.TextXAlignment = Enum.TextXAlignment.Center
    titleLabel.Size = UDim2.new(1, -80, 1, 0)
    titleLabel.Position = UDim2.new(0, 40, 0, 0)
    titleLabel.BackgroundTransparency = 1
    titleLabel.Parent = titleBar
    
    -- Close button (горизонтальна лінія)
    local closeBtn = Instance.new("TextButton")
    closeBtn.Text = "—"
    closeBtn.Size = UDim2.new(0, 18, 0, 18)
    closeBtn.Position = UDim2.new(1, -20, 0, 2)
    closeBtn.BackgroundColor3 = OS2_COLORS.ButtonFace
    closeBtn.Parent = titleBar
    
    return dialog
end

================================================================================
2.3 ЗАГАЛЬНІ ПРАВИЛА ДЛЯ ДІАЛОГОВИХ ВІКОН
================================================================================

2.3.1 ВИБІР СТИЛЮ:
-------------------
- Основні діалоги: Windows 95 Style
- Системні повідомлення: Windows 95 Style
- Налаштування/Options: Можна OS/2 Warp Style
- Спеціальні вікна: На вибір розробника

2.3.2 СТРУКТУРА ВМІСТУ:
------------------------
Frame "DialogWindow"
├── Frame "TitleBar" (Height: 20px)
│   ├── ImageLabel "Icon" (16x16)
│   ├── TextLabel "Title"
│   ├── TextButton "MinimizeBtn" -- "_"
│   ├── TextButton "MaximizeBtn" -- "□"
│   └── TextButton "CloseBtn" -- "X"
├── Frame "MenuBar" (опціонально)
│   ├── TextButton "File"
│   └── TextButton "Help"
├── Frame "Content"
│   └── [Вміст вікна]
└── Frame "StatusBar" (опціонально)
    └── TextLabel "Status"

2.3.3 ЕФЕКТ 3D РАМКИ:
----------------------
-- Функція для створення 3D рамки (працює для обох стилів)
local function create3DBorder(frame, raised, style)
    style = style or "WIN95"
    local colors = style == "OS2" and OS2_COLORS or WIN95_COLORS
    
    local light = raised and colors.ButtonHighlight or colors.ButtonShadow
    local dark = raised and colors.ButtonShadow or colors.ButtonHighlight
    
    -- Верхня і ліва рамка (світла для raised)
    -- Нижня і права рамка (темна для raised)
end

2.3.4 ТИПИ ДІАЛОГОВИХ ВІКОН:
-----------------------------

a) ІНФОРМАЦІЙНЕ ВІКНО (MessageBox):
   - Іконка: "i" в синьому колі
   - Кнопка: [OK]
   
b) ПОПЕРЕДЖЕННЯ (Warning):
   - Іконка: "!" в жовтому трикутнику
   - Кнопки: [OK] або [Yes] [No]
   
c) ПОМИЛКА (Error):
   - Іконка: "X" в червоному колі
   - Кнопка: [OK]
   
d) ПІДТВЕРДЖЕННЯ (Confirm):
   - Іконка: "?" в синьому колі
   - Кнопки: [Yes] [No] або [OK] [Cancel]

2.3.5 ПРИКЛАД КОДУ ДІАЛОГУ WIN95:
----------------------------------
local function createWin95Dialog(title, message, dialogType)
    local dialog = Instance.new("Frame")
    dialog.BackgroundColor3 = WIN95_COLORS.WindowBackground
    dialog.BorderSizePixel = 0
    dialog.Size = UDim2.new(0, 300, 0, 150)
    dialog.Position = UDim2.new(0.5, -150, 0.5, -75)
    
    -- Title bar
    local titleBar = Instance.new("Frame")
    titleBar.BackgroundColor3 = WIN95_COLORS.TitleBarActive
    titleBar.Size = UDim2.new(1, 0, 0, 20)
    titleBar.Parent = dialog
    
    -- Title text
    local titleLabel = Instance.new("TextLabel")
    titleLabel.Text = title
    titleLabel.TextColor3 = WIN95_COLORS.TitleText
    titleLabel.Font = Enum.Font.SourceSansBold
    titleLabel.Parent = titleBar
    
    -- ... continue with content
    
    return dialog
end

================================================================================
3. ПІДКАЗКИ ТА СПОВІЩЕННЯ (TOASTS / NOTIFICATIONS)
================================================================================

3.1 ТИПИ СПОВІЩЕНЬ:
--------------------

a) TOAST (короткочасне повідомлення):
   - Позиція: Нижній правий кут
   - Тривалість: 3-5 секунд
   - Анімація: Slide in → Fade out
   - Використання: "Локацію відкрито!", "Телепортація завершена"

b) NOTIFICATION (важливе сповіщення):
   - Позиція: Верхній центр
   - Тривалість: 5-10 секунд або до закриття
   - Кнопка закриття: Так
   - Використання: "Нова планета доступна!", "Досягнення розблоковано"

c) TOOLTIP (підказка при наведенні):
   - Позиція: Біля курсора
   - Затримка появи: 0.5 секунди
   - Тривалість: Поки курсор над об'єктом
   - Використання: Опис кнопок, об'єктів

3.2 СТИЛІЗАЦІЯ СПОВІЩЕНЬ:
--------------------------

-- Кольори за типом
local NOTIFICATION_COLORS = {
    Success = {
        Background = Color3.fromRGB(39, 174, 96),   -- Зелений
        Icon = "✅",
    },
    Error = {
        Background = Color3.fromRGB(231, 76, 60),   -- Червоний
        Icon = "❌",
    },
    Warning = {
        Background = Color3.fromRGB(241, 196, 15),  -- Жовтий
        Icon = "⚠️",
    },
    Info = {
        Background = Color3.fromRGB(52, 152, 219),  -- Синій
        Icon = "ℹ️",
    },
    Achievement = {
        Background = Color3.fromRGB(155, 89, 182),  -- Фіолетовий
        Icon = "🏆",
    },
}

3.3 СТРУКТУРА TOAST:
---------------------
Frame "Toast"
├── ImageLabel "Icon" (emoji або зображення)
├── TextLabel "Message"
└── Frame "ProgressBar" (опціонально, для таймера)

Параметри:
- Ширина: 250-350px
- Висота: 50-80px
- Закруглення: 8px
- Тінь: DropShadow
- Шрифт: SourceSansBold, 14-16px

3.4 ПРИКЛАД TOAST:
-------------------
local function showToast(message, toastType, duration)
    duration = duration or 3
    
    local toast = Instance.new("Frame")
    toast.BackgroundColor3 = NOTIFICATION_COLORS[toastType].Background
    toast.Size = UDim2.new(0, 300, 0, 60)
    toast.Position = UDim2.new(1, 320, 1, -80) -- Start off-screen
    toast.AnchorPoint = Vector2.new(1, 1)
    
    -- Slide in animation
    local tweenIn = TweenService:Create(toast, TweenInfo.new(0.3), {
        Position = UDim2.new(1, -20, 1, -80)
    })
    
    -- Fade out after duration
    task.delay(duration, function()
        local tweenOut = TweenService:Create(toast, TweenInfo.new(0.3), {
            Position = UDim2.new(1, 320, 1, -80)
        })
        tweenOut:Play()
        tweenOut.Completed:Wait()
        toast:Destroy()
    end)
    
    tweenIn:Play()
end

3.5 ЧЕРГА СПОВІЩЕНЬ:
---------------------
- Максимум 3 toast одночасно
- Нові сповіщення зсувають попередні вгору
- FIFO порядок (перше прийшло - перше зникло)

================================================================================
4. МЕНЮ ТА НАВІГАЦІЯ
================================================================================

4.1 ГОЛОВНЕ МЕНЮ (MAIN MENU):
------------------------------
Стиль: Windows 95 з космічною тематикою

Структура:
Frame "MainMenu"
├── Frame "TitleBar" -- "KOSMICMAZER - Головне меню"
├── Frame "MenuContent"
│   ├── TextButton "ResumeGame" -- "▶ Продовжити"
│   ├── TextButton "Settings" -- "⚙ Налаштування"
│   ├── TextButton "Achievements" -- "🏆 Досягнення"
│   ├── TextButton "Help" -- "❓ Допомога"
│   └── TextButton "ExitGame" -- "🚪 Вийти"
└── Frame "Footer"
    └── TextLabel "Version" -- "v1.0.0"

4.2 СТИЛЬ КНОПОК МЕНЮ:
-----------------------
- Розмір: Широкі (200-250px), висота 35-40px
- Ефект hover: Інверсія кольорів (Windows 95 стиль)
- Ефект press: 3D "втиснутий" вигляд
- Іконки: Emoji або піксельні іконки зліва від тексту

4.3 ПІДМЕНЮ (SUBMENU):
-----------------------
- Випадають справа від батьківського пункту
- Стрілка "►" вказує на наявність підменю
- Затримка появи: 0.2 секунди

================================================================================
5. СТАТУС БАРИ (STATUS BARS)
================================================================================

5.1 ГОЛОВНИЙ СТАТУС БАР (HUD):
-------------------------------
Позиція: Верхній лівий кут або нижня частина екрану

Обов'язкові елементи:
- Поточна локація (назва)
- Поточна планета (назва + іконка)
- Час у грі (опціонально)

Структура:
Frame "HUD"
├── Frame "LocationInfo"
│   ├── ImageLabel "PlanetIcon"
│   ├── TextLabel "PlanetName"
│   └── TextLabel "LocationName"
├── Frame "PlayerStats" (опціонально)
│   └── TextLabel "PlayTime"
└── Frame "QuickActions"
    └── ImageButton "MenuButton"

5.2 СТИЛІЗАЦІЯ HUD:
--------------------
- Напівпрозорий фон (0.5 прозорості)
- Закруглені кути (4-8px)
- Мінімалістичний дизайн
- Не перекривати ігрову зону

5.3 ПРОГРЕС БАРИ:
------------------
Використання:
- Завантаження локації
- Cooldown телепортації
- Час сканування

Стиль:
- Висота: 10-20px
- Фон: Темний (#333333)
- Заповнення: Градієнт або суцільний колір
- Рамка: 1px темна або 3D ефект (Win95)

================================================================================
6. СПЕЦІАЛЬНІ ІНТЕРФЕЙСИ
================================================================================

6.1 ТЕЛЕПОРТАЦІЙНИЙ ІНТЕРФЕЙС:
-------------------------------
Реалізація: StarterPlayerScripts/TeleportationClient.client.luau

Структура:
Frame "TeleportUI" (Win95 стиль)
├── Frame "TitleBar" -- "Система телепортації"
├── ScrollingFrame "LocationList"
│   └── [Кнопки локацій]
├── Frame "Preview" (опціонально)
│   └── ImageLabel "LocationImage"
├── Frame "Actions"
│   ├── TextButton "TeleportBtn" -- "Телепортуватись"
│   └── TextButton "CancelBtn" -- "Скасувати"
└── Frame "StatusBar"
    └── TextLabel "Status"

Кнопки локацій:
- Відкриті: Звичайний стиль, клікабельні
- Закриті: Сірий текст, іконка замка 🔒
- Поточна: Виділена синім фоном

6.2 СКАНЕР ПЛАНЕТ:
-------------------
Реалізація: StarterPlayerScripts/PlanetSurfaceScannerClient.client.luau

Елементи:
- Анімована рамка сканування
- Прогрес бар сканування
- Результати: Список знайдених локацій
- Візуальні ефекти: Пульсація, частинки

6.3 ІНВЕНТАР (якщо буде):
--------------------------
Стиль: Windows 95 вікно
- Сітка предметів (grid layout)
- Drag & drop підтримка
- Tooltip при наведенні
- Контекстне меню (правий клік)

================================================================================
7. АНІМАЦІЇ ТА ПЕРЕХОДИ
================================================================================

7.1 СТАНДАРТНІ АНІМАЦІЇ:
-------------------------
- Fade In/Out: 0.2-0.3 секунди
- Slide In/Out: 0.3-0.5 секунди
- Scale (поява): 0.2 секунди
- Bounce (підтвердження): 0.1 секунди

7.2 EASING ФУНКЦІЇ:
--------------------
- Меню: Quad.Out (плавне сповільнення)
- Діалоги: Back.Out (легкий "відскок")
- Закриття: Quad.In (прискорення)
- Натискання: Linear (миттєво)

7.3 ПРИКЛАД АНІМАЦІЇ:
----------------------
local TweenService = game:GetService("TweenService")

-- Поява вікна
local function showWindow(window)
    window.Size = UDim2.new(0, 0, 0, 0)
    window.Visible = true
    
    local tween = TweenService:Create(
        window,
        TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
        {Size = UDim2.new(0, 300, 0, 200)}
    )
    tween:Play()
end

-- Закриття вікна
local function hideWindow(window)
    local tween = TweenService:Create(
        window,
        TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.In),
        {Size = UDim2.new(0, 0, 0, 0)}
    )
    tween:Play()
    tween.Completed:Wait()
    window.Visible = false
end

================================================================================
8. ДОСТУПНІСТЬ (ACCESSIBILITY)
================================================================================

8.1 КОНТРАСТНІСТЬ:
-------------------
- Мінімальний контраст тексту: 4.5:1
- Важливі елементи: 7:1
- Уникати червоний/зелений як єдині індикатори

8.2 РОЗМІРИ:
-------------
- Мінімальний розмір тексту: 12px
- Мінімальний розмір кнопок: 40x40px для touch
- Достатній відступ між елементами

8.3 КЛАВІАТУРНА НАВІГАЦІЯ:
---------------------------
- Tab для переходу між елементами
- Enter для активації
- Escape для закриття/скасування

8.4 ПІДКАЗКИ:
--------------
- Всі іконки мають текстовий опис
- Важливі дії мають підтвердження

================================================================================
9. АДАПТИВНІСТЬ (RESPONSIVE DESIGN)
================================================================================

9.1 ТОЧКИ ПЕРЕЛОМУ:
--------------------
- Mobile: < 600px
- Tablet: 600-1024px
- Desktop: > 1024px

9.2 ПРАВИЛА АДАПТАЦІЇ:
-----------------------
- Використовувати UDim2 з відносними значеннями де можливо
- Тестувати на різних розмірах екрану
- Меню може змінювати layout на мобільних
- Кнопки більші на touch devices

9.3 ПРИКЛАД АДАПТИВНОГО РОЗМІРУ:
---------------------------------
local function getAdaptiveSize()
    local viewport = workspace.CurrentCamera.ViewportSize
    
    if viewport.X < 600 then
        return "Mobile"
    elseif viewport.X < 1024 then
        return "Tablet"
    else
        return "Desktop"
    end
end

================================================================================
10. NAMING CONVENTIONS ДЛЯ GUI
================================================================================

10.1 НАЗВИ ЕЛЕМЕНТІВ:
----------------------
- ScreenGui: [Name]Screen (LoadingScreen, GameScreen)
- Frame: [Name]Container, [Name]Panel, [Name]Window
- TextLabel: [Name]Label, [Name]Text
- TextButton: [Name]Btn, [Name]Button
- ImageLabel: [Name]Icon, [Name]Image
- ImageButton: [Name]IconBtn

10.2 ПРИКЛАДИ:
---------------
- LoadingScreen
- TeleportWindow
- LocationListContainer
- TeleportBtn
- CloseBtn
- PlanetIcon
- StatusLabel

================================================================================
]]

local module = {}

module.VERSION = "1.0"
module.STATUS = "ACTIVE"
module.LAST_UPDATED = "2026-01-07"

-- Експорт кольорів Windows 95
module.WIN95_COLORS = {
    WindowBackground = Color3.fromRGB(192, 192, 192),
    TitleBarActive = Color3.fromRGB(0, 0, 128),
    TitleBarInactive = Color3.fromRGB(128, 128, 128),
    TitleText = Color3.fromRGB(255, 255, 255),
    WindowText = Color3.fromRGB(0, 0, 0),
    ButtonFace = Color3.fromRGB(192, 192, 192),
    ButtonHighlight = Color3.fromRGB(255, 255, 255),
    ButtonShadow = Color3.fromRGB(128, 128, 128),
}

-- Експорт кольорів сповіщень
module.NOTIFICATION_COLORS = {
    Success = Color3.fromRGB(39, 174, 96),
    Error = Color3.fromRGB(231, 76, 60),
    Warning = Color3.fromRGB(241, 196, 15),
    Info = Color3.fromRGB(52, 152, 219),
    Achievement = Color3.fromRGB(155, 89, 182),
}

return module
